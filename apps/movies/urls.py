from django.urls import path
from rest_framework.urlpatterns import format_suffix_patterns


app_name = 'apps.movies'

urlpatterns = [
    #path('movies/', ArticleList.as_view()),
]

#urlpatterns = format_suffix_patterns(urlpatterns)

